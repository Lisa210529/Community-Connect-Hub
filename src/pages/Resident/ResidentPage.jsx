export default function ResidentPage() {
  return (
    <section className="page-section active">
      <div className="card-custom">
        <div className="card-header">Resident Dashboard</div>
        <div className="card-body">
          <p className="text-muted">
            Submit community requests, track projects, and view ward announcements.
          </p>
          <p className="mb-0 small text-secondary">
            Migrate features from <code>legacy/js/resident.js</code> into this module.
          </p>
        </div>
      </div>
    </section>
  );
}
